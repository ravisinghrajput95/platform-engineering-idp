# CloudCart Frontend

Frontend web application for the CloudCart platform, built with React and Vite.

!!! note
    The `ravisinghrajput95/cloudcart-frontend` repository doesn't exist yet. This page is
    placeholder documentation hosted alongside the catalog entry so the component has a
    working TechDocs tab; once the real repo exists, move this content there and switch its
    `backstage.io/techdocs-ref` annotation from `dir:../../techdocs/cloudcart-frontend` back
    to `url:https://github.com/ravisinghrajput95/cloudcart-frontend/tree/main/`.

## Stack

- **Language/framework**: React, Vite
- **Runtime**: containerized, deployed to the `cloudcart-dev` GKE cluster
- **Depends on**: `cloudcart-backend` (see the catalog entry's `dependsOn`)

## Pages

- `/` -- landing page
- `/login` -- GitHub OAuth sign-in

(Illustrative -- update once the real application's routes are documented.)
